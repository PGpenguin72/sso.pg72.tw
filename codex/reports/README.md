# Reports

The assignee and the task's named reviewer each write a separate immutable,
role-qualified report using the report template. A review report must identify
the exact reviewed snapshot and never overwrite the assignee's report.
