# Tasks

Only A creates immutable task files here. A task is assigned only when its file
names the role, exact base, branch and worktree. Scope changes are mailbox
amendments, not edits to the original task.
